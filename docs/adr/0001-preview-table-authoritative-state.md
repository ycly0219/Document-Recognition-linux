# 预览表格权威状态归属纯模块

预览表格的全部预览单据、当前选中单据、拆分结构、行命令与撤销记录由无 Tk 的 `PreviewTable` 模块持有，Tk 只渲染其不可变快照并转发命令。选择该方案是为了让预览业务可以脱离界面独立测试，并让导出与 WMS 共用同一份权威数据；继续由 `EditableTreeview` 持有权威状态迁移更少，但会把业务规则、校验和下游数据组装继续耦合在控件上。
